{
    "builds": [{
        "src": "Backend/NoteCanvas/NoteCanvas/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "Backend/NoteCanvas/NoteCanvas/wsgi.py"
        }
    ]
}
